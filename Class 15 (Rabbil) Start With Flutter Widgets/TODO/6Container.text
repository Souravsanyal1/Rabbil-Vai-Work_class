class HomePage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Flutter B4",style: TextStyle(color: Colors.blue),),),
      body: Center(
        child: Container(
          width: 200,
          height: 200,
          color: Colors.amber,
          child: Image.network("https://rabbil.com/files/rabbilVai1.webp?v=1"),
        )
      ),
    );
  }
}
